import GuestDashboard from "../../react_pages/guest/GuestDashboard";

export const metadata = {
    title: "Guest Mode — Demo Dashboard | NutriKids",
    description: "Explore the NutriKids pediatric nutrition and child growth tracking platform in Guest Demo Mode.",
};

export default function Page() {
    return <GuestDashboard />;
}
