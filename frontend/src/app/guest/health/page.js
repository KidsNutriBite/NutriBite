import GuestDashboard from "../../../react_pages/guest/GuestDashboard";

export const metadata = {
    title: "Manage Health (Demo) — Guest Mode | NutriKids",
    description: "View child health, growth tracking, food journal, hydration, and medical background in demo mode.",
};

export default function Page() {
    return <GuestDashboard />;
}
